<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Activity.FullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="CustomSeekBar">
        <item name="android:progressDrawable">@drawable/progress_bar_bg</item>
        <item name="android:thumb">@drawable/oval</item>
    </style>

    <style name="CustomSeekBarWhite">
        <item name="android:progressDrawable">@drawable/progress_bar_white_bg</item>
        <item name="android:thumb">@drawable/oval_white</item>
    </style>

</resources>
